<UserControl x:Class="RentalClient.UserControls.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:RentalClient.Core"
             mc:Ignorable="d"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc"
             xmlns:converters="http://sharpvectors.codeplex.com/svgc/"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" 
                    Style="{StaticResource LogoBorderStyle}" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left">
    
                <Border.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="{Binding HomeCommand}" />
                </Border.InputBindings>
    
                <converters:SvgViewbox Source="/Themes/Resources/Logo.svg" 
                       Height="80"
                       Stretch="Uniform" />
            </Border>
            
            <TextBox Grid.Column="1" 
                     Style="{StaticResource RoundTextBoxStyle}" 
                     Tag="ПОШУК ЗА МІСТОМ/АДРЕСОЮ..."
                     HorizontalAlignment="Stretch" VerticalContentAlignment="Center" 
                     Margin="30,0" 
                     core:Designer.IsMonitoring="True"
                     Text="{Binding SearchState.SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding PerformGlobalSearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            
            <Button Grid.Column="2" 
                    Content="Профіль" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Command="{Binding ProfileCommand}" 
                    VerticalAlignment="Center" />
            
        </Grid>
        
        <ContentControl Grid.Row="1" Content="{Binding CurrentWorkplaceView}" />
    </Grid>
</UserControl>