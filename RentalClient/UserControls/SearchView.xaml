<UserControl x:Class="RentalClient.UserControls.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RentalClient.UserControls"
             xmlns:core="clr-namespace:RentalClient.Core"
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <core:NgrokImageConverter x:Key="NgrokConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" 
                Style="{StaticResource CardBorderStyle}" 
                MinWidth="280" HorizontalAlignment="Left">
            <StackPanel Margin="15">
                <Label Style="{StaticResource SectionTitleLabelStyle}" 
                       FontWeight="Heavy" Content="Фільтр" Margin="0,0,0,15"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Label Grid.Row="0" Grid.Column="0" Style="{StaticResource SectionTitleLabelStyle}" Content="Ціна" Margin="0,0,10,10"/>
                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox MinWidth="60" Style="{StaticResource FilterTextBoxStyle}" Text="{Binding MinPrice, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Style="{StaticResource SectionTitleLabelStyle}" Content="-" Margin="5,0"/>
                        <TextBox MinWidth="60" Style="{StaticResource FilterTextBoxStyle}" Text="{Binding MaxPrice, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    
                    <Label Grid.Row="1" Grid.Column="0" Style="{StaticResource SectionTitleLabelStyle}" Content="Кімнати:" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FilterTextBoxStyle}" Margin="2,10" Text="{Binding Rooms, UpdateSourceTrigger=PropertyChanged}"/>
                    
                    <Label Grid.Row="2" Grid.Column="0" Style="{StaticResource SectionTitleLabelStyle}" Content="Тип:" Margin="0,0,10,10"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
                        <ToggleButton MinWidth="120" Style="{StaticResource FilterToggleButtonStyle}" Content="Квартира" IsChecked="{Binding IsApartment}"/>
                        <ToggleButton MinWidth="120" Style="{StaticResource FilterToggleButtonStyle}" Content="Будинок" IsChecked="{Binding IsHouse}"/>
                    </StackPanel>
                    
                    <Label Grid.Row="3" Grid.Column="0" Style="{StaticResource SectionTitleLabelStyle}" Content="Площа:" Margin="0,0,10,10"/>
                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox MinWidth="60" Style="{StaticResource FilterTextBoxStyle}" Text="{Binding MinSpace, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Style="{StaticResource SectionTitleLabelStyle}" Content="-" Margin="5,0"/>
                        <TextBox MinWidth="60" Style="{StaticResource FilterTextBoxStyle}" Text="{Binding MaxSpace, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>
                
                <Button Content="ЗАСТОСУВАТИ" 
                        Style="{StaticResource PrimaryButtonStyle}" 
                        Command="{Binding ApplyFilterCommand}"
                        Margin="0,20,0,0"/>
                
                <Button Content="ОЧИСТИТИ" 
                        Style="{StaticResource PrimaryButtonStyle}" 
                        Command="{Binding ClearFiltersCommand}"
                        Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <Label Style="{StaticResource SectionTitleLabelStyle}" 
                       Content="{Binding Rentals.Count}" 
                       ContentStringFormat="Результати пошуку: {0}"/>
                <StackPanel Orientation="Horizontal">
                    <Label Style="{StaticResource SectionTitleLabelStyle}" Content="Сортувати за:"/>
                    <RadioButton GroupName="Sort" Style="{StaticResource TabRadioButtonStyle}" Content="Ціна" IsChecked="{Binding PriceSort}"/>
                    <RadioButton GroupName="Sort" Style="{StaticResource TabRadioButtonStyle}" Content="Площа" IsChecked="{Binding SpaceSort}"/>
                    <RadioButton GroupName="Sort" Style="{StaticResource TabRadioButtonStyle}" Content="Кількість кімнат" IsChecked="{Binding RoomsCountSort}"/>
                    <ToggleButton Style="{StaticResource SortDirectionToggleButtonStyle}"
                                  IsChecked="{Binding IsSortDescending}" 
                                  Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>
            <Grid Grid.Row="1">
                <ListBox ItemsSource="{Binding Rentals}" 
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden">
                     
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}" 
                                MinWidth="250" Height="300" MaxWidth="450" Margin="10" Padding="10"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" 
                                              Command="{Binding DataContext.OpenListingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                              CommandParameter="{Binding Id}" />
                            </Border.InputBindings>
                            <StackPanel>
                                <Border Height="180" BorderBrush="{StaticResource BorderPrimaryBrush}" BorderThickness="1" CornerRadius="10" Margin="0,0,0,15">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding ImageUrl, Converter={StaticResource NgrokConverter}, IsAsync=True}" 
                                                    Stretch="UniformToFill" />
                                    </Border.Background>
                                </Border>
                                
                                <TextBlock Text="{Binding Price, FallbackValue='Ціна'}" HorizontalAlignment="Center" FontWeight="Heavy" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding Title, FallbackValue='Назва'}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding City, FallbackValue='Місто'}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
                
                <Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
          
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" 
                                     Width="100" Height="10" 
                                     Margin="0,0,0,15"
                                     Foreground="#A0C8FC" Background="Transparent" BorderThickness="0"/>
            
                        <TextBlock Text="Завантаження..." 
                                   FontWeight="Bold" Foreground="#555555" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
        
    </Grid>
</UserControl>